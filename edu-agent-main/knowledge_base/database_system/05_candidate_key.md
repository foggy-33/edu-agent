# 候选码

## 知识点概述
候选码是能唯一标识关系中元组的最小属性集。

## 核心概念
- 超码：能唯一标识元组的属性集。
- 候选码：不含多余属性的超码。
- 主属性：出现在任一候选码中的属性。

## 常见易错点
- 超码可以包含冗余属性，候选码不能包含冗余属性。
- 复合候选码需要检查每个真子集是否仍能决定全部属性。

## 示例
R(A,B,C), F={A->B, B->C}，A+={A,B,C}，A 是候选码。

## 练习题
给定 R(A,B,C,D), F={A->B, B->C, C->D}，判断 A 是否为候选码。
