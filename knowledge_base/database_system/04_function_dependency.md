# 函数依赖

## 知识点概述
函数依赖描述属性之间的决定关系，是候选码和范式判断的基础。

## 核心概念
- X->Y：若 X 的值确定，则 Y 的值也确定。
- 平凡函数依赖：Y 是 X 的子集。
- 属性闭包 X+：由 X 根据依赖集能推出的全部属性。

## 常见易错点
- X->Y 不表示因果关系，只表示数据约束中的唯一决定关系。
- 判断候选码时必须验证最小性。

## 示例
若 student_id->name，则同一个 student_id 不应对应多个 name。

## 练习题
给定 F={A->B, B->C}，求 A+。
